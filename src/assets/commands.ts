// import cmd from './commands.json';

// const pingName = cmd.commands[0].ping.commandName;
// const pingDesc = cmd.commands[0].ping.commandDesc;
// const embedName = cmd.commands[1]['send-embed'].commandName;
// const embedDesc = cmd.commands[1]['send-embed'].commandDesc;

// const cmdNames = [pingName, embedName];
// const cmdDesc = [pingDesc, embedDesc];

// for (let i = 0; i < cmdNames.length; i++) {

// }